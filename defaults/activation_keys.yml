default_subscription: []

rhel7_ak:
  - name: "act-os-rhel-7Server"
    lifecycle_environment: "Library"
    content_view: 'cv-os-rhel7'
    subscriptions: "{{ default_subscription }}"
#      - name: "{{ default_subscription }}"
    content_overrides:
      - label: rhel-7-server-optional-rpms
        override: enabled
    auto_attach: False
    create: True

rhel8_ak:
  - name: "act-os-rhel-8Server"
    lifecycle_environment: "Library"
    content_view: 'cv-os-rhel8'
    subscriptions:
      #- name: "Red Hat Enterprise Linux Server, Standard (1 Virtual Node)"
      - name: "{{ default_subscription }}"
    content_overrides:
      - label: rhel-8-server-optional-rpms
        override: enabled
    auto_attach: False
    create: True

ansible_ak:
  - name: "act-app-ansible"
    create: False
